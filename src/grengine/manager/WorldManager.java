package grengine.manager;

import java.util.ArrayList;

import grengine.physics.Vec3;
import grengine.player.Player;




public abstract class WorldManager {

/*
	public static boolean VectorInWorld(Vec3 vec) {
		
		if (vec.x < WorldManager.xMin || vec.x > WorldManager.xMax) return false;
		if (vec.y < WorldManager.yMin || vec.y > WorldManager.yMax) return false;
		return true;
	}
	
	
	*/
}
